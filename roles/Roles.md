# Roles

- **Project Manager (`role_manager`):** Full privileges on tasks — create, read, update, delete, and assign.
- **Team Member (`role_member`):** Limited privileges — read and update only tasks assigned to them; cannot create, delete, or reassign tasks.
